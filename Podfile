# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Review for multiple-choice exercises' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'HandyJSON', '~> 5.0.2'
  pod 'SwiftOverlays', '~> 5.0.0'
  pod 'Charts'
  pod 'MessageKit'
 pod 'Alamofire', '~> 4.6'

  # Pods for Review for multiple-choice exercises

  target 'Review for multiple-choice exercisesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Review for multiple-choice exercisesUITests' do
    # Pods for testing
  end

end
